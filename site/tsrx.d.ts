/// <reference types="vite/client" />

// `.tsrx` modules: editor IntelliSense comes from the Ripple/TSRX VSCode
// extension (Volar). This ambient declaration is the tsc/CLI fallback.
// `?raw` / `?url` / env typings come from `vite/client` above — no per-suffix
// declaration needed.
declare module '*.tsrx';
